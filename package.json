{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Um projeto para a pratica de Clean Architecture",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:ci": "npm test -- --coverage --silent",
    "test:wacth": "jest --watchAll",
    "test:staged": "npm test -- --findRelatedTests --silent",
    "test:coveralls": "npm run test:ci && coveralls < coverage/lcov.info",
    "lint-staged": "lint-staged"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "luanpontes2@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "husky": "^4.0.0-beta.5",
    "jest": "^24.9.0",
    "lint-staged": "^9.4.3",
    "standard": "^14.3.1"
  }
}
